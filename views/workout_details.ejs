<!doctype html>
<html>
  <head>
    <title><%= workout.workout_name %> | <%= appData.appName %></title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
  </head>

  <body>
    <!--Top navigation bar-->
    <header class="top-banner">
        <a class="site-name" href="/">FitTrack</a>

        <nav>
            <a href="/">Home</a>
            <a href="/exercises/list">Exercises</a>
            <a href="/workouts">Workout Plans</a>
            <a href="/external/meals">Meal Ideas</a>
            <a href="/about">About</a>
            <a href="/users/login">Login</a>
        </nav>
    </header>
    <!--Display workout information-->
    <h1><%= workout.workout_name %></h1>
    
    <!--Display a message if no exercises have been added-->
    <% if (exercises.length === 0) { %>

    <p>No exercises added yet.</p>

    <% } else { %>

        <!--Display all exercises assigned to this workout-->
        <% exercises.forEach(function(exercise) { %>
    
            <div class="exercise-card">
                <h3><%= exercise.name %></h3>
                <p><strong>Muscle group:</strong> <%= exercise.muscle_group %></p>
                <p><strong>Sets:</strong> <%= exercise.set_count %></p>
                <p><strong>Reps:</strong> <%= exercise.rep_count %></p>
                
                <form method="POST"
                action="/workouts/<%= workout.id %>/exercises/<%= exercise.id %>/delete">

                <button type="submit">Remove Exercise</button>
            </form>
            </div>

            

        <% }) %>

    <% } %>
    <a href="/workouts/<%= workout.id %>/add">
        Add Exercises
    </a>
    <p>
        <a href="/workouts">Return to workout plans</a>
    </p>

  </body>
</html>